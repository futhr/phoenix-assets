# Elixir build artifacts
/_build/
/deps/
/.elixir_ls/
/cover/
/doc/
*.beam
*.ez
erl_crash.dump

# JavaScript
node_modules/
/npm/*/dist/
/npm/*/coverage/
*.tsbuildinfo

# SvelteKit (adapter-static) build output -- hashed, never committed; appears
# when an integration/install test scaffolds an app that builds into priv/static.
priv/static/_app/immutable/

# Editors / OS
.DS_Store
*.iml
